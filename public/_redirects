# Netlify redirects for SPA
/*    /index.html   200

# Ensure OAuth callbacks work
/auth/callback    /index.html   200
/auth/reset-password    /index.html   200

# Handle hash-based OAuth redirects
/#*    /index.html   200

# API routes (if any)
/api/*    /api/:splat   200

# Ensure all routes go to index.html for client-side routing
/dashboard    /index.html   200
/settings    /index.html   200
